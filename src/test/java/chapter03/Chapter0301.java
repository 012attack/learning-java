package chapter03;

import org.junit.jupiter.api.Test;

public class Chapter0301 {

    // 산술 +, -, *, /, %(나머지)
    // 부호 +, -
    // 문자열 +
    // 대입 =, +=, -=, *=, /=, %=
    // 증감 ++, --
    // 비교 ==, !=, >, <, >=, <=, instanceof
    // 논리 !, &, |, &&, ||
    // 조건(삼항연산자) (조건식) ? A : B (ture : false)
    @Test
    void c0301_연산자(){

    }
}
